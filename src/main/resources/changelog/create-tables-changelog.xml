<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">
    <changeSet id="schoodule-tables/v1/01-06-2025/1" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.school(
                id bigserial PRIMARY KEY,
                name varchar NOT NULL,
                is_deleted boolean DEFAULT FALSE
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/01-06-2025/2" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.schedule(
                id bigserial PRIMARY KEY,
                school_id bigint NOT NULL,
                name varchar NOT NULL,
                is_deleted boolean DEFAULT FALSE,
                CONSTRAINT fk_school_schedule_id
                    FOREIGN KEY (school_id)
                        REFERENCES public.school(id)
                        ON DELETE RESTRICT
                        ON UPDATE CASCADE
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/01-06-2025/3" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.building(
               id bigserial PRIMARY KEY,
               school_id bigint NOT NULL,
               name varchar NOT NULL,
               is_deleted boolean NOT NULL DEFAULT FALSE,
               CONSTRAINT fk_school_building_id
                   FOREIGN KEY (school_id)
                       REFERENCES public.school(id)
                       ON DELETE RESTRICT
                       ON UPDATE CASCADE
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/01-06-2025/4" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.cabinet(
               id bigserial PRIMARY KEY,
               building_id bigint NOT NULL,
               name varchar NOT NULL,
               is_deleted boolean NOT NULL DEFAULT FALSE,
               CONSTRAINT fk_building_cabinet_id
                   FOREIGN KEY (building_id)
                       REFERENCES public.building(id)
                       ON DELETE RESTRICT
                       ON UPDATE CASCADE
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/01-06-2025/5" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.teacher(
               id bigserial PRIMARY KEY,
               school_id bigint NOT NULL,
               name varchar NOT NULL,
               is_deleted boolean DEFAULT FALSE,
               CONSTRAINT fk_school_teacher_id
                   FOREIGN KEY (school_id)
                       REFERENCES public.school(id)
                       ON DELETE RESTRICT
                       ON UPDATE CASCADE
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/01-06-2025/6" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.subject(
               id bigserial PRIMARY KEY,
               school_id bigint NOT NULL,
               name varchar NOT NULL,
               is_deleted boolean DEFAULT FALSE,
               CONSTRAINT fk_school_subject_id
                   FOREIGN KEY (school_id)
                       REFERENCES public.school(id)
                       ON DELETE RESTRICT
                       ON UPDATE CASCADE
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/01-06-2025/7" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.school_class(
                id bigserial PRIMARY KEY,
                school_id bigint NOT NULL,
                grade integer NOT NULL,
                litera varchar NOT NULL,
                is_deleted boolean DEFAULT FALSE,
                CONSTRAINT fk_school_school_class_id
                    FOREIGN KEY (school_id)
                        REFERENCES public.school(id)
                        ON DELETE RESTRICT
                        ON UPDATE CASCADE
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/02-02-2025/1" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.user(
                id bigserial PRIMARY KEY,
                username varchar(100) NOT NULL UNIQUE,
                password varchar(255) NOT NULL,
                email varchar(255) NOT NULL UNIQUE,
                created_at timestamptz NOT NULL DEFAULT NOW(),
                corporate boolean NOT NULL DEFAULT FALSE,
                school_id bigint,
                deleted timestamptz,
                CONSTRAINT fk_user_school_id
                    FOREIGN KEY (school_id)
                        REFERENCES public.school(id)
                        ON DELETE CASCADE
                        ON UPDATE CASCADE
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/02-02-2025/4" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.role(
                id bigserial PRIMARY KEY,
                name role_type NOT NULL UNIQUE,
                description varchar(255)
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/02-02-2025/5" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            INSERT INTO public.role (name, description) VALUES
                ('ADMIN', 'Администратор системы'),
                ('DIRECTOR', 'Директор школы'),
                ('DEPUTY_DIRECTOR', 'Заместитель директора (завуч)'),
                ('TEACHER', 'Учитель'),
                ('STUDENT', 'Ученик'),
                ('BASIC_MAKER', 'Базовый составитель'),
                ('ADVANCED_MAKER', 'Продвинутый составитель'),
                ('PRO_MAKER', 'Профессиональный составитель'),
                ('VIEWER', 'Просмотрщик');
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/02-02-2025/6" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.user_role(
                 id bigserial PRIMARY KEY,
                 user_id bigint NOT NULL,
                 role_id bigint NOT NULL,
                 assigned_at timestamptz NOT NULL DEFAULT NOW(),
                 CONSTRAINT fk_user_role_user_id
                     FOREIGN KEY (user_id)
                         REFERENCES public.user(id)
                         ON DELETE CASCADE
                         ON UPDATE CASCADE,
                 CONSTRAINT fk_user_role_role_id
                     FOREIGN KEY (role_id)
                         REFERENCES public.role(id)
                         ON DELETE CASCADE
                         ON UPDATE CASCADE,
                 UNIQUE (user_id, role_id)
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schedule-planning/v1/02-03-2025/1" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE class_curriculum (
                id bigserial PRIMARY KEY,
                schedule_id bigint NOT NULL,
                school_class_id bigint NOT NULL,
                subject_id bigint NOT NULL,
                hours_per_week integer NOT NULL CHECK (hours_per_week > 0),
                CONSTRAINT fk_curriculum_schedule
                  FOREIGN KEY (schedule_id)
                      REFERENCES schedule(id)
                      ON DELETE CASCADE,
                CONSTRAINT fk_curriculum_class
                  FOREIGN KEY (school_class_id)
                      REFERENCES school_class(id)
                      ON DELETE RESTRICT,
                CONSTRAINT fk_curriculum_subject
                  FOREIGN KEY (subject_id)
                      REFERENCES subject(id)
                      ON DELETE RESTRICT,
                UNIQUE (schedule_id, school_class_id, subject_id)
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/03-13-2026/1" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.login_attempt(
                id bigserial PRIMARY KEY,
                user_id bigint NOT NULL,
                time timestamptz NOT NULL,
                success boolean NOT NULL,
                CONSTRAINT fk_user_id
                    FOREIGN KEY (user_id)
                        REFERENCES public.user(id)
                        ON DELETE CASCADE
                        ON UPDATE CASCADE
            );
            ]]>
        </sql>
    </changeSet>
    <changeSet id="schoodule-tables/v1/03-18-2026/subscription" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.subscription (
                id          bigserial   PRIMARY KEY,
                user_id     bigint      NOT NULL UNIQUE,
                plan        varchar(50) NOT NULL DEFAULT 'BASIC',
                expires_at  timestamptz,
                created_at  timestamptz NOT NULL DEFAULT NOW(),
                updated_at  timestamptz NOT NULL DEFAULT NOW(),
                CONSTRAINT fk_subscription_user
                    FOREIGN KEY (user_id)
                        REFERENCES public.user(id)
                        ON DELETE CASCADE
                        ON UPDATE CASCADE,
                CONSTRAINT chk_subscription_plan
                    CHECK (plan IN ('BASIC','ADVANCED','PRO','VIEWER'))
            );
            CREATE TABLE public.subscription_plan_roles (
                plan        varchar(50) NOT NULL,
                role_name   role_type   NOT NULL,
                PRIMARY KEY (plan, role_name),
                CONSTRAINT chk_spr_plan
                    CHECK (plan IN ('BASIC','ADVANCED','PRO','VIEWER'))
            );
            INSERT INTO public.subscription_plan_roles(plan, role_name) VALUES
                ('BASIC',    'BASIC_MAKER'),
                ('ADVANCED', 'ADVANCED_MAKER'),
                ('ADVANCED', 'BASIC_MAKER'),
                ('PRO',      'PRO_MAKER'),
                ('PRO',      'ADVANCED_MAKER'),
                ('PRO',      'BASIC_MAKER'),
                ('VIEWER',   'VIEWER');
            /* ── Migrate existing PERSONAL users only (corporate = false) ──
               Corporate users (teachers, directors, etc.) are managed
               by their school administrator and never use subscriptions.
               Personal users already have BASIC_MAKER from registration,
               so we only insert the subscription row. */
            /*INSERT INTO public.subscription(user_id, plan, expires_at)
            SELECT id, 'BASIC', NULL
            FROM public.user
            WHERE corporate = false
              AND deleted IS NULL
                ON CONFLICT (user_id) DO NOTHING;*/
            ]]>
        </sql>
    </changeSet>
    <changeSet id="federal-curriculum/v1/05-14-2026/tables" author="Shabakhov_Eset">
        <sql>
            <![CDATA[
            CREATE TABLE public.federal_curriculum (
                id bigserial PRIMARY KEY,
                title varchar NOT NULL,
                education_level education_level_type NOT NULL,
                study_week_type study_week_type NOT NULL,
                version varchar NOT NULL,
                academic_year varchar(9) NOT NULL,
                description varchar,
                is_deleted boolean NOT NULL DEFAULT FALSE,
                created_at timestamptz NOT NULL DEFAULT NOW(),
                updated_at timestamptz NOT NULL DEFAULT NOW(),
                CONSTRAINT chk_federal_curriculum_academic_year
                    CHECK (academic_year ~ '^[0-9]{4}/[0-9]{4}$')
            );

            CREATE TABLE public.federal_curriculum_requirement (
                id bigserial PRIMARY KEY,
                federal_curriculum_id bigint NOT NULL,
                grade integer NOT NULL,
                subject_name varchar NOT NULL,
                weekly_hours integer NOT NULL,
                part_type curriculum_part_type NOT NULL,
                is_deleted boolean NOT NULL DEFAULT FALSE,
                created_at timestamptz NOT NULL DEFAULT NOW(),
                updated_at timestamptz NOT NULL DEFAULT NOW(),
                CONSTRAINT fk_federal_curriculum_requirement_curriculum
                    FOREIGN KEY (federal_curriculum_id)
                        REFERENCES public.federal_curriculum(id)
                        ON DELETE CASCADE
                        ON UPDATE CASCADE,
                CONSTRAINT chk_federal_curriculum_requirement_grade
                    CHECK (grade BETWEEN 1 AND 11),
                CONSTRAINT chk_federal_curriculum_requirement_weekly_hours
                    CHECK (weekly_hours > 0)
            );
            ]]>
        </sql>
    </changeSet>
</databaseChangeLog>
